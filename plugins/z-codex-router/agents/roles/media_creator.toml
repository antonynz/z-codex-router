id = "media_creator"
model = "{{model}}"
reasoning_effort = "{{reasoning_effort}}"
task_scope = "{{task_scope}}"
owned_paths = "{{owned_paths}}"
acceptance = "{{acceptance}}"
permission = "May create assigned media artifacts; does not make publication or rights decisions."
