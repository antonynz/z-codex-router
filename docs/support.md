# Support

For a reproducible issue, include the `routerctl` error code, sanitized operating-system details, the command action, and a minimal temporary fixture. Do not include tokens, private `config.toml` content, or personal paths.

Before filing an issue, run the Router Doctor skill. It is read-only and reports managed-block, profile, and runtime compatibility state.
