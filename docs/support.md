# 支持

提交可复现问题时，请包含 `routerctl` 错误码、脱敏操作系统信息、command action 与最小临时 fixture。
不要包含 token、私有 `config.toml` 内容或个人路径。

提交 issue 前先运行 Router Doctor skill。它是只读操作，会报告 managed block、profile 与 runtime
compatibility state。
